…or create a new repository on the command line

echo "# Todo-api" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/aliaburas80/Todo-api.git
git push -u origin master
…or push an existing repository from the command line

git remote add origin https://github.com/aliaburas80/Todo-api.git
git push -u origin master



 git reset HEAD data/dev-todo-api.sqlite   // to remove file or folder from git

 git commit --amend -am ""   --amend two commit and smaching togother


heroku addons:create heroku-postgresql:hobby-dev // to add addons to heroku
heroku pg:wait // wait until create db


